services:
    fastapi:
        build:
            context: ./api
            dockerfile: Dockerfile
        ports:
            - "8000:8000"
        env_file: ./api/.env
    streamlit:
        build:
            context: ./web
            dockerfile: Dockerfile
        ports:
            - "8501:8501" 
        env_file: ./web/.env
        depends_on:
            - fastapi